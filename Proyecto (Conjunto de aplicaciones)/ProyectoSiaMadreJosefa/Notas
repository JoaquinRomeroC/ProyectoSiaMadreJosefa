Codigos:

TERMINAL
-Para correr el proyecto:
python manage.py runserver

-Para instalar una base de datos:
python -m pip install psycopg2

-Para crear tablas de configuracion en pgAdmin 4 (Actualizar movimientos en sql)
python manage.py makemigrations
python manage.py migrate

-Crear un usuario:
python manage.py createsuperuser

MODELS

-Para indicar que es un texto de Caracteres
nombreVariable: models.Charfield (Max_length = CantidadDeLetrasPermitidas, null= (False = no se puede dejar en blanco; True = Se puede dejar en blanco), Blank = (False True), primary_key = True)

VIWS

-Aqui se muestran todas las vistas que tendra el sistema

URLS

-Aqui se muestran las direcciones urls qe estaran vinculadas con las VIWS, ademas se pueden separar en diferentes aplicaciones por temas de orden.
-Cuando se separan en aplicaciones hay que detacar que las urls de la carpeta MADRE debera contener todas las urls de las distintas aplicaciones.

HTML

-Aqui es donde se hara el codigo para las vistas, el cual tiene que ser enlazado con las viws

EJEMPLO
def ejemplo2(request):
    return  render(request,'nombreDelArchivoTemplate')